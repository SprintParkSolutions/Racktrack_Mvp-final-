# Development / CI-only Python deps.
#
# Kept separate from requirements.txt because that file is what the Dockerfile
# installs into the shipped image — a test runner has no business in
# production. Install both when working on the pipeline:
#     pip install -r requirements.txt -r requirements-dev.txt
#
# Run the pipeline unit tests from the REPO ROOT (the modules import each other
# as `pipeline.*`, so the root must be on sys.path):
#     python -m pytest pipeline/tests -q

pytest>=8.0.0
pytest-cov>=5.0.0      # test-coverage measurement

# Code-standards & security tooling for the Python pipeline (config in pyproject.toml):
#   ruff      — PEP 8 formatting + linting (+ import order, security, docstrings)
#   mypy      — static type checking (lenient to start)
#   bandit    — Python security linter
#   pip-audit — dependency-vulnerability scanning
ruff>=0.6.0
mypy>=2.3.1
bandit[toml]>=1.7.0
pip-audit>=2.7.0
