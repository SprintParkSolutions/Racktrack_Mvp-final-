# RackTrack — public demo deployment (demo.racktrack.ai).
#
#   docker compose -f docker-compose.demo.yml up -d --build
#
# Standalone rather than an override of docker-compose.yml, deliberately:
# list-valued keys like `ports` MERGE across -f files rather than replace, so
# an override file cannot un-publish :3001. The app would stay reachable over
# plain HTTP behind Caddy's back — exactly the thing this file exists to
# prevent. One self-contained file has no such trap.
#
# Differences from docker-compose.yml (the local/dev one):
#   * Caddy terminates TLS and is the only published surface (:80, :443).
#   * The app is not published at all — it is reachable only on the private
#     compose network, so nothing can bypass TLS.
#   * CORS is pinned to the demo origin.
#   * server/data is persisted; the local file omits it, which is fine for a
#     throwaway run and would silently wipe the demo on every redeploy.
#
# Setup, DNS and first-run steps: docs/setup/DEMO-VPS-SETUP.md

services:
  racktrack:
    build:
      context: .
      dockerfile: Dockerfile
    image: racktrack:demo
    # NOT "racktrack": the VPS is shared and already runs a container by that
    # name (racktrack:latest, published on :8095, untouched by this file).
    # Compose would refuse to start on the name collision, and any attempt to
    # force past it would take down a deployment that is not ours.
    container_name: racktrack-demo

    # Secrets (JWT, SMTP, SSH creds) — never baked into the image.
    env_file:
      - server/.env

    environment:
      - PORT=3001
      # Scan reports are rendered server-side, so their timestamps come out in
      # the CONTAINER's zone. Unset, that is UTC, and every report read 5h30m
      # behind the technicians who took the scan — invisible on the build Mac,
      # which is already in the team's zone. Set both: TZ fixes container-wide
      # time (logs, cron), REPORT_TZ pins what reports print regardless of host.
      - TZ=${TZ:-Asia/Kolkata}
      - REPORT_TZ=${REPORT_TZ:-Asia/Kolkata}
      # Must be exactly this string. app.js's .env loader does not strip
      # inline comments, so a trailing "# ..." makes it !== "production" and
      # the server quietly stays in dev mode, leaking raw errors publicly.
      - NODE_ENV=production
      # Each worker holds the whole torch + model stack resident (~1.5–2.5 GB),
      # so this tracks free RAM, and separately it should not exceed the core
      # count — inference is CPU-bound, and a third worker on a 2-core box just
      # makes all three slower.
      #
      # TWO is the ceiling here: the box has 2 vCPUs. It only fits once the
      # older :8095 RackTrack instance is stopped, which frees the ~2 GB that
      # forced this to 1 originally. Without that, two workers plus node sit on
      # the memory ceiling and the container is OOM-killed mid-scan — a failure
      # that presents as a pipeline bug and is miserable to trace back to RAM.
      #
      # Going beyond 2 means a bigger plan, not a bigger number: KVM 4 for 3,
      # KVM 8 for 5.
      # One worker on this box. Each worker holds every model plus the OCR
      # reader (3-4 GB resident); two of them alongside NetBox pushed the
      # 7.8 GB VPS into OOM kills mid-scan, which is what "the rack takes
      # forever" was. One worker answers a scan in one pass instead.
      - RACKTRACK_WORKERS=1
      - OMP_NUM_THREADS=2
      # app.js reads this exact name (not CORS_ORIGINS). It must match the
      # browser's origin or every API call from the demo site is refused.
      - CORS_ALLOWED_ORIGINS=https://demo.racktrack.ai

    # Deliberately `expose`, not `ports` — reachable from Caddy, not the world.
    expose:
      - "3001"

    volumes:
      # Model weights — git-ignored, ~483 MB, rsync'd to the host separately.
      - ./Models:/app/Models:ro
      - ./active_learning_Cache/data:/app/active_learning_Cache/data
      - ./outputs:/app/outputs
      # auth.db and jwt.secret live here. Without this mount every redeploy
      # signs out every account and drops every scan the demo has collected.
      - ./server/data:/app/server/data
      # Microsoft Graph mail identity: token/mailid.md (the tenant + client ids)
      # and token/support_msal_cache.json (the support mailbox's refresh token).
      # MOUNTED rather than baked into the image — these are credentials, and
      # the image is built from the same context on every redeploy.
      #
      # Without them graphMail cannot resolve a tenant id, every send returns
      # false, and the SMTP fallback below has nothing configured either — so
      # the server accepted a forgot-password request, answered 200 and sent
      # NOTHING. Signup verification fails the same way; seeded testers never
      # noticed because their accounts skipped it.
      - ./token:/app/token:ro

    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://localhost:3001/healthz',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 30s
      timeout: 5s
      retries: 3
      # Two workers each load every model from a cold page cache on a CPU-only
      # box; 40s (the local file's value) marks the container unhealthy while
      # it is still legitimately starting.
      start_period: 120s

    restart: unless-stopped

  caddy:
    image: caddy:2-alpine
    container_name: racktrack-caddy
    depends_on:
      - racktrack
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      # Certificates and the ACME account key. Lose this volume and Caddy
      # re-issues on every restart, which will hit Let's Encrypt's rate limit
      # (5 certs per domain per week) and leave the demo without TLS for days.
      - caddy_data:/data
      - caddy_config:/config
    restart: unless-stopped

volumes:
  caddy_data:
  caddy_config:
