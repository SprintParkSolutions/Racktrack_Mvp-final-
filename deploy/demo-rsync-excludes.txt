# rsync excludes for pushing this checkout to the demo VPS.
#
#   rsync -avz --progress --exclude-from=deploy/demo-rsync-excludes.txt \
#     ./ root@82.29.164.213:/opt/racktrack-demo/
#
# Why this exists rather than `git clone` on the box: the repo is private and
# the VPS has no GitHub credentials. Why it exists rather than reusing
# .dockerignore: rsync cannot read that file, and its `!negation` syntax means
# the two are not interchangeable anyway.
#
# Takes the checkout from ~2 GB to ~16 MB. The big offenders are not obvious:
# .venv alone is ~900 MB (a Windows virtualenv carrying torch's DLLs, which are
# useless on Linux and get reinstalled from the CPU wheel index in the image
# anyway), and server.zip is another 106 MB.
#
# Models/ is excluded here and sent separately — see docs/setup/DEMO-VPS-SETUP.md.
# Keep this list in step with .dockerignore when that changes.

# Virtualenvs — platform-specific binaries, rebuilt inside the image
.venv
venv
__pycache__
*.pyc

# Dependencies — reinstalled from lockfiles during the build
node_modules

# Version control
.git

# Weights and runtime data — mounted as volumes, transferred separately
Models
outputs
batch_outputs
active_learning_Cache
*.zip

# Mobile build artifacts — the image serves the web build only
client/ios
client/android
client/build
client/dist

# Host-specific and secret. server/data holds production's auth.db; server/.env
# holds live lab and third-party credentials. Neither belongs on a public box.
server/data
server/.env
server/.env.key
# Azure app-registration ids pasted in at the repo root during handover. The
# ids themselves are not secret and the same values ship inside token/mailid.md
# by design — these two are simply loose copies that have no business on an
# internet-facing host. rsync does not read .gitignore, so ignoring them there
# was not enough to keep them off the VPS.
id.md
supportid.md
# Local-only working directory (gitignored). Never belonged on the VPS: it
# carries internal collateral AND, under _local/archive/, copies of the MSAL
# token caches the patterns below were written to keep off this host. Those
# patterns are anchored with a leading `/`, so they only ever matched the repo
# ROOT — `_local/archive/racktrack_teams_cache.json` sailed straight past them.
# Excluding the whole directory closes that hole at the source.
_local

# Handover copies of MSAL token caches dropped at the repo root. The real ones
# live at pipeline/racktrack_teams_cache.json and token/support_msal_cache.json
# and DO ship — the senders need them. These duplicates carry the same live
# refresh tokens and must not be scattered further.
/*_teams_cache*.json
/*_teams_cache*
/*_outlook_cache*.json
# The tester roster: 30 real email addresses paired with plaintext passwords.
# rsync does not read .gitignore, so without this line it ships to the VPS on
# every deploy — a credential file on an internet-facing host that the running
# app never reads. Only seed-testers.js and mail-credentials.js use it, and
# both are run from a workstation. Copy it up by hand if you ever need to
# re-seed on the box, and delete it afterwards.
server/scripts/tester-roster.json
server/feedback
server/uploads

# Not needed at runtime
docs
Test_Image
retraining_learning
netdisco-docker
# servicenow/ must ship: /api/topology spawns topology_generate.py from it.
# Its bulk does not.
servicenow/mock_scans
servicenow/__pycache__
servicenow_inbox
racktrack-support-bot
support-bot
cmdb_racks
e2e
